import { CoursePlateDirective } from './course-plate.directive';

describe('CoursePlateDirective', () => {
  it('should create an instance', () => {
    const directive = new CoursePlateDirective(null);
    expect(directive).toBeTruthy();
  });
});
